# wallaby-jest-config-problem


How to replicate the problem

```
npm install
npx @wallabyjs/cli --doNotOpen --config wallaby.js 
```

But it works with automatic config

```
npx @wallabyjs/cli --doNotOpen 

```
